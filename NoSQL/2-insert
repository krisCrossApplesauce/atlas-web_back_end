// write a script that inserts a document in the collection called school:
// the document must have one attribute called name with the value "Holberton school"
// the database name will be passed as option of mongo command
var db = process.argv[3]
db.school.insertOne({name: "Holberton school"})
